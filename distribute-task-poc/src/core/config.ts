const config = {
    region: 'westeurope',
    cosmos: {
        endpoint: 'https://distributedtaskpoc.documents.azure.com:443/',
        key: '<your key>',
        database: 'POC',
        collection: 'Store'
    }
};

export default config;